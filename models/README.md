# Model artifacts

Trained artifacts are generated locally and are not committed by default. Each model card must include training period, excluded periods, features, sample size, validation design, metrics, limitations, and the source build identifier.
